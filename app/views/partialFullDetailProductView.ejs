<? if(shoppingCart && shoppingCart.length > 0) { ?>
    <table class="table table-striped">
        <thead>
        <tr>
            <th>#</th>
            <th>Product Name</th>
            <th>Description</th>
            <th>Price</th>
            <th>Quantity</th>
            <th>Total Cost</th>
            <th>Remove</th>
        </tr>
        </thead>
        <? shoppingCart.forEach(function(product) { ?>
        <tr>
            <td><?= product.id ?></td>
            <td><?= product.name ?></td>
            <td><?= product.description ?></td>
            <td>£<?= product.price ?></td>
            <th><?= product.quantity ?></th>
            <th>£<?= product.quantity  * parseInt(product.price, 0) ?></th>
            <td><a href="/products/remove/<?= product.id ?>">Remove From Cart</a></td>
        </tr>
        <? }); ?>
    </table>
<? } else { ?>
    <p><i>Empty Shopping Cart.</i></p>
<? } ?>